# 03 — 기능 상세 명세

> **문서 상태**: 초안 (Draft)
> **작성일**: 2026-09-05
> **버전**: v0.1
> **연관 문서**: `02-user-stories.md`

---

## 목차

- [모듈 A — 영수증 처리](#module-a)
- [모듈 B — 세금계산서 발행](#module-b)
- [모듈 C — 도면 설계](#module-c)
- [모듈 D — 제품 카탈로그](#module-d)
- [모듈 X — 공통](#module-x)

---

<a id="module-a"></a>

## 모듈 A — 영수증 처리

### A-1. 영수증 등록

#### 입력
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 이미지 | File (JPG·PNG·PDF) | 선택 | 영수증 원본 사진 또는 스캔본. 최대 10MB |
| 날짜 | Date | ✅ | 영수증 발행일 (기본값: 오늘) |
| 금액 | Number | ✅ | 원 단위 정수. 음수 불가 |
| 거래처 | String | ✅ | 직접 입력 또는 거래처 주소록에서 선택 |
| 항목 분류 | Enum | ✅ | 재료비 / 운반비 / 인건비 / 장비비 / 기타 |
| 메모 | String | 선택 | 최대 200자 |

#### 출력
- 저장된 영수증 ID, 등록 일시
- 목록 화면으로 이동 또는 "계속 등록" 선택 가능

#### 비즈니스 룰
- Free 플랜: 월 누적 등록 건수 20건 초과 시 등록 불가 → 업그레이드 유도 메시지 표시
- 이미지 없이 금액·날짜만으로도 등록 가능 (이미지는 선택)
- 등록자 정보(직원 ID) 자동 기록
- 날짜는 미래 날짜 허용하지 않음

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 이미지 10MB 초과 | 업로드 전 클라이언트 측 차단, 안내 메시지 |
| 지원하지 않는 파일 형식 | 업로드 차단, 허용 형식 안내 |
| 금액 0 이하 입력 | 저장 불가, 인라인 에러 표시 |
| Free 플랜 한도 초과 | 저장 차단, 플랜 업그레이드 팝업 |
| 네트워크 오류 | 입력 데이터 로컬 임시 보관 후 재시도 안내 |

---

### A-2. 영수증 목록 조회

#### 입력 (필터)
| 필터 | 타입 | 기본값 |
|------|------|--------|
| 기간 (시작일~종료일) | DateRange | 이번 달 1일~오늘 |
| 거래처 | String (검색) | 전체 |
| 항목 분류 | Enum (다중 선택) | 전체 |
| 등록자 | 직원 선택 | 전체 (관리자만 가능) |

#### 출력
- 필터 조건에 맞는 영수증 목록 (페이지당 20건)
- 목록 하단: 조회된 건수 및 금액 합계
- 각 항목: 날짜 · 거래처 · 금액 · 항목 분류 · 이미지 유무 · 등록자

#### 비즈니스 룰
- 직원은 본인이 등록한 건만 조회 가능
- 관리자는 전체 조회 가능
- 최대 조회 기간: 1년 (성능 보장)
- 목록 기본 정렬: 날짜 내림차순

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 조회 결과 없음 | "해당 조건의 영수증이 없습니다" 메시지 |
| 1,000건 초과 조회 | 경고 후 엑셀 내보내기 권장 안내 |

---

### A-3. 영수증 수정·삭제

#### 비즈니스 룰
- 직원: 본인 등록 건, 등록 후 24시간 이내만 수정 가능
- 관리자: 전체 건 기간 제한 없이 수정·삭제 가능
- 삭제는 소프트 삭제(soft delete) — DB에서 실제 삭제하지 않고 deleted_at 기록
- 삭제된 건은 목록에서 미표시, 합계 계산에서 제외

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 직원이 24시간 초과 건 수정 시도 | 수정 불가 안내, 관리자에게 요청 유도 |
| 삭제 확인 다이얼로그 취소 | 삭제 취소, 원래 화면 유지 |

---

### A-4. 영수증 내보내기

#### 입력
- 현재 적용된 필터 조건 그대로 사용
- 출력 형식 선택: PDF / 엑셀(xlsx)

#### 출력 — PDF
- 표지: 업체명, 기간, 생성 일시
- 본문 테이블: 날짜·거래처·항목·금액
- 마지막 페이지: 항목별 소계 및 총계

#### 출력 — 엑셀
- 시트 1: 영수증 목록 (원본 데이터)
- 시트 2: 항목별 집계 (피벗)

#### 비즈니스 룰
- Pro 이상 플랜 전용
- 최대 1,000건까지 단일 파일 내보내기. 초과 시 기간을 나눠 내보내도록 안내

---

<a id="module-b"></a>

## 모듈 B — 세금계산서 발행

### B-1. 거래처 등록·관리

#### 입력
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 사업자번호 | String (10자리) | ✅ | 하이픈 없이 입력, 자동 포맷 |
| 상호 | String | ✅ | 최대 100자 |
| 대표자명 | String | ✅ | |
| 업태 | String | ✅ | |
| 종목 | String | ✅ | |
| 사업장 주소 | String | ✅ | |
| 이메일 | Email | 선택 | 발행 알림용 |
| 담당자명 | String | 선택 | |
| 담당자 연락처 | String | 선택 | |

#### 비즈니스 룰
- 동일 업체 내 사업자번호 중복 등록 불가
- 사업자번호 10자리 숫자 형식 검증 (체크섬은 검증하지 않음 — 입력 책임은 사용자)
- 거래처 삭제 시 해당 거래처로 발행된 세금계산서가 있으면 삭제 불가 → 비활성화로 전환

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 사업자번호 중복 | 기존 거래처 정보 표시 후 수정 여부 확인 |
| 10자리 미만 입력 | 저장 불가, 인라인 에러 |
| 세금계산서 있는 거래처 삭제 시도 | 삭제 불가 안내, 비활성화 권장 |

---

### B-2. 세금계산서 작성

#### 입력
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 작성일자 | Date | ✅ | 기본값: 오늘 |
| 공급자 | 자사 정보 자동 입력 | — | 업체 설정에서 관리 |
| 공급받는자 | 거래처 선택 | ✅ | 거래처 주소록에서 검색 선택 |
| 품목 | Array | ✅ | 최소 1개 이상 |
| └ 품목명 | String | ✅ | |
| └ 규격 | String | 선택 | |
| └ 수량 | Number | ✅ | 양의 정수 |
| └ 단가 | Number | ✅ | 원 단위 |
| 비고 | String | 선택 | 최대 200자 |

#### 자동 계산
```
공급가액 = 수량 × 단가  (품목별)
세액 = 공급가액 × 10%  (부가세율 고정)
합계 = 공급가액 합계 + 세액 합계
```

#### 출력 — 홈택스 엑셀 파일 규격
홈택스 전자세금계산서 bulk 업로드 양식에 맞춘 xlsx 파일 생성.

| 컬럼 | 값 |
|------|----|
| 종류 | 세금계산서 |
| 공급자 등록번호 | 자사 사업자번호 |
| 공급자 상호 | 자사 상호 |
| 공급받는자 등록번호 | 거래처 사업자번호 |
| 공급받는자 상호 | 거래처 상호 |
| 작성일자 | YYYY-MM-DD |
| 품목 (최대 16행) | 품목명·규격·수량·단가·공급가액·세액 |
| 합계금액 | 공급가액 합계 |
| 세액합계 | 세액 합계 |

#### 비즈니스 룰
- 품목 최대 16개 (홈택스 bulk 양식 제한)
- 작성 완료 후 상태: `작성완료`
- 엑셀 다운로드 시 상태: `엑셀다운로드완료`로 변경
- 임시 저장 시 상태: `임시저장` — 목록에서 표시되나 집계에서 제외
- 동일 내용 재발행 시 기존 건 복사 후 신규 작성으로 처리

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 품목 없이 저장 시도 | 저장 불가, 안내 메시지 |
| 품목 17개 이상 추가 시도 | 추가 불가, 16개 제한 안내 |
| 수량·단가에 음수 입력 | 저장 불가, 인라인 에러 |
| 거래처 미선택 | 저장 불가 |

---

### B-3. 세금계산서 발행 이력 조회

#### 입력 (필터)
| 필터 | 기본값 |
|------|--------|
| 기간 | 이번 달 |
| 거래처 | 전체 |
| 상태 | 전체 (임시저장 / 작성완료 / 엑셀다운로드완료 / 취소) |

#### 출력
- 목록: 작성일자·거래처·합계금액·세액·상태·다운로드 버튼
- 상단 집계: 이번 달 발행 건수 / 공급가액 합계 / 세액 합계

---

### B-4. 세금계산서 수정·취소

#### 수정
- 기존 세금계산서를 원본으로 복사 → 수정 후 신규 건으로 저장
- 원본 건은 `수정발행됨` 상태로 변경 (삭제 아님)

#### 취소
- 취소 사유 입력 필수 (최대 100자)
- 취소 후 상태: `취소` → 집계에서 제외
- 이미 홈택스에 업로드한 건의 취소는 시스템 외부(홈택스)에서 별도 처리 필요 — 안내 문구 표시

---

<a id="module-c"></a>

## 모듈 C — 도면 설계

### C-1. 도면 생성

#### 입력
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 도면명 | String | ✅ | 최대 100자 |
| 고객명 | String | 선택 | |
| 현장 주소 | String | 선택 | |
| 캔버스 폭 | Number (cm) | ✅ | 실측 기준. 최소 100cm |
| 캔버스 높이 | Number (cm) | ✅ | 실측 기준. 최소 100cm |
| 그리드 간격 | Number (cm) | 선택 | 기본값: 50cm |

#### 비즈니스 룰
- Pro 플랜: 보관 도면 최대 5개. 초과 시 생성 불가 → 삭제 또는 Business 업그레이드 안내
- Business 플랜: 무제한
- 생성 즉시 버전 1로 저장

---

### C-2. 도면 편집 (Fabric.js)

#### 심볼 라이브러리 — 기본 제공 목록
| 카테고리 | 심볼 |
|----------|------|
| 가열 기구 | 가스레인지 2구·4구·6구, 인덕션, 튀김기, 그리들 |
| 냉장·냉동 | 업소용 냉장고, 업소용 냉동고, 쇼케이스 |
| 세척 | 싱크대 1조·2조·3조, 식기세척기 |
| 환기 | 직화 후드, 벽부형 후드, 환풍기 |
| 작업대 | 작업대 (폭 60·90·120·150cm), 선반, 이동식 카트 |
| 기타 | 출입문, 창문, 기둥, 벽 |

#### 심볼 소스 — 기본 제공 vs 제품 카탈로그 연동
- 기본 제공 심볼(위 표)은 프론트엔드에 내장된 정적 SVG 라이브러리로, DB 테이블 없이 카테고리별 아이콘 하나만 관리한다.
- 특정 제품(SKU, 모듈 D)을 도면에 배치하고 싶을 때는 제품 카탈로그의 `icon_svg_url`(탑다운 심볼, 목록용 정면 사진 `photo_url`과 별도 관리)을 가져와 표시하고, 배치된 오브젝트에 `productId`를 함께 저장한다. 냉장고처럼 종류가 여러 개인 제품도 이 경로로 각자의 심볼을 가질 수 있다.

#### 편집 기능
| 기능 | 설명 |
|------|------|
| 심볼 추가 | 사이드바(기본 심볼 + 제품 카탈로그 검색)에서 클릭 또는 드래그 앤 드롭으로 캔버스에 배치 |
| 이동 | 심볼 드래그 |
| 크기 조절 | 핸들 드래그 (비율 고정 옵션) |
| 회전 | 회전 핸들 드래그 또는 각도 직접 입력 |
| 라벨 추가 | 심볼 더블클릭 → 텍스트 입력 |
| 복사·붙여넣기 | Ctrl+C / Ctrl+V |
| 실행 취소·다시 실행 | Ctrl+Z / Ctrl+Y (최대 30단계) |
| 그리드 스냅 | 설정에서 ON/OFF |
| 줌·팬 | 마우스 휠 줌, 스페이스+드래그 팬 |
| 객체 잠금 | 선택 후 잠금 → 이동·수정 불가 |
| 레이어 순서 | 앞으로·뒤로 이동 |

#### 저장 방식
- Fabric.js `canvas.toJSON(['label', 'symbolType', 'locked', 'productId', 'heightMm', 'sillHeightMm', 'headHeightMm'])` 결과를 DB에 저장
  - 뒤 4개 커스텀 필드는 2D 편집에는 쓰이지 않지만, 추후 3D 압출(extrude) 시 이 값을 그대로 활용하기 위해 지금부터 저장해둔다 (`05-database-schema.md` 참고)
  - `productId`가 있으면 해당 오브젝트는 제품 카탈로그 SKU와 연결된 것 — 렌더링 시 카탈로그의 `icon_svg_url`로 아이콘을 그린다
- 자동 저장: 마지막 변경 후 30초 경과 시 자동 저장 (임시 버전)
- 수동 저장: 저장 버튼 클릭 → 새 버전으로 기록

---

### C-3. 버전 관리

#### 버전 정책
- 수동 저장마다 버전 번호 1씩 증가
- 자동 저장은 임시 버전으로 별도 관리 (버전 번호 미부여)
- 보관 버전 수: 최대 20개. 초과 시 가장 오래된 버전 자동 삭제

#### 복원
- 특정 버전 선택 → 미리보기 → 복원 확인
- 복원 시 현재 상태를 새 버전으로 저장 후 선택 버전 내용으로 덮어씀

---

### C-4. PDF 출력 및 고객 공유

#### PDF 출력
- 출력 크기: A4 / A3 선택
- 포함 정보: 도면명, 고객명, 현장 주소, 작성일, 업체 로고(설정 시), 도면 이미지
- 해상도: 300dpi 이상
- Fabric.js `canvas.toDataURL({ format: 'png', multiplier: 3 })` → PDF 변환 (jsPDF 사용)

#### 고객 공유 링크
- 링크 생성 시 고유 토큰 발급 (UUID 기반)
- 링크 접근 시 로그인 불필요, 읽기 전용 뷰어 제공
- 만료일 설정: 7일 / 30일 / 만료 없음 선택
- 링크 비활성화(무효화) 가능
- 고객 뷰어: 도면 이미지 + 도면명·고객명·날짜 표시. 편집 불가

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 만료된 링크 접근 | "만료된 링크입니다" 안내 페이지 |
| 비활성화된 링크 접근 | "더 이상 유효하지 않은 링크입니다" |
| 빈 캔버스 PDF 출력 시도 | 심볼 없을 시 경고 후 진행 여부 확인 |

---

<a id="module-d"></a>

## 모듈 D — 제품 카탈로그

### D-1. 제품 등록

#### 입력
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 제품명 | String | ✅ | 최대 200자 |
| 사진 | File[] | 선택 | 최대 5장, 각 10MB 이하, JPG·PNG |
| 대분류 | String | ✅ | 예: 그릇, 조리도구, 주방 설비 |
| 중분류 | String | ✅ | |
| 소분류 | String | 선택 | |
| 재질 | String | 선택 | 예: 스테인리스, 알루미늄, 도자기 |
| 용도 | String | 선택 | 최대 500자 |
| 사이즈 | Array | 선택 | S/M/L/XL 또는 직접 입력 (cm) |
| 단가 | Number | 선택 | 원 단위 |
| 바코드 번호 | String | 선택 | 제조사 바코드 (13자리 EAN 등) |
| 도면용 아이콘 | File (SVG) | 선택 | 탑다운(위에서 본) 심볼. 미지정 시 카테고리 기본 심볼(모듈 C) 사용. 목록용 정면 사진과는 별도 파일 |
| 특이사항·주의사항 | String | 선택 | 최대 500자 |

#### 비즈니스 룰
- 저장 시 시스템 내부 제품 코드 자동 생성 (예: `PRD-000001`)
- 바코드 번호 입력 시 동일 업체 내 중복 불가
- 대분류·중분류 신규 입력 가능 (자유 입력 + 기존 목록 자동완성)

#### 예외 처리
| 케이스 | 처리 |
|--------|------|
| 바코드 중복 | 기존 제품 링크 표시 후 확인 요청 |
| 사진 5장 초과 | 추가 불가 안내 |
| 사진 10MB 초과 | 업로드 차단 |

---

### D-2. 텍스트 검색·조회

#### 검색 대상 필드
- 제품명, 대/중/소분류, 재질, 용도, 특이사항

#### 필터
- 대분류 선택
- 사이즈 (S/M/L/XL 다중 선택)
- 재질

#### 출력
- 검색 결과 목록: 썸네일 이미지, 제품명, 카테고리, 대표 단가
- 상세 페이지: 전체 사진 슬라이더, 사이즈별 스펙 테이블, 단가, 용도, 주의사항

#### 비즈니스 룰
- 모바일 최적화 UI 필수 (하단 고정 검색창, 카드형 목록)
- 검색 결과 없을 시 유사 카테고리 제품 추천
- 조회 이력 기록 → 대시보드 "자주 조회된 제품" 집계에 사용

---

### D-3. 바코드·QR 스캔 조회

#### 지원 코드 유형
| 유형 | 설명 |
|------|------|
| EAN-13 / EAN-8 | 제조사 기존 바코드 |
| QR코드 | 시스템 자체 생성 QR (`PRD-{제품코드}` 인코딩) |
| Code 128 | 일부 업소용 제품 바코드 |

#### 플로우
1. 앱에서 스캔 버튼 탭 → 카메라 실행
2. 코드 인식 → 제품 DB 조회
3. 매칭 성공: 제품 상세 팝업 표시
4. 매칭 실패: "등록되지 않은 제품입니다" + 제품 등록 바로가기(관리자만 표시)

#### 비즈니스 룰
- 스캔은 웹 카메라 API 사용 (별도 앱 설치 불필요)
- 조회 성공 시 해당 제품의 조회 카운트 +1 기록

---

### D-4. 제품 수정·삭제

#### 단가 변경 이력
- 단가 수정 시 이전 단가·변경일·변경자 이력 자동 기록
- 이력은 관리자만 조회 가능

#### 삭제
- 소프트 삭제 (deleted_at 기록)
- 삭제된 제품의 바코드·QR 스캔 시 "단종 또는 삭제된 제품" 안내

---

<a id="module-x"></a>

## 모듈 X — 공통

### X-1. 업체 계정 생성

#### 입력
| 필드 | 필수 | 설명 |
|------|------|------|
| 업체명 | ✅ | |
| 사업자번호 | ✅ | 10자리, 중복 불가 |
| 대표자명 | ✅ | |
| 이메일 | ✅ | 로그인 ID로 사용 |
| 비밀번호 | ✅ | 최소 8자, 영문+숫자 조합 |
| 연락처 | 선택 | |

#### 비즈니스 룰
- 이메일 인증 필수 (가입 후 인증 메일 발송 → 24시간 내 인증 완료)
- 인증 전 상태: 로그인 가능하나 데이터 저장 기능 제한
- 가입 즉시 Free 플랜 적용

---

### X-2. 직원 초대 및 권한

#### 플로우
1. 관리자가 이메일 입력 + 역할(관리자/직원) 선택 → 초대 버튼
2. 시스템이 초대 링크 포함 이메일 발송 (유효기간 72시간)
3. 초대받은 직원이 링크 클릭 → 비밀번호 설정 → 가입 완료
4. 해당 업체 계정에 자동 소속

#### 권한 매트릭스
| 기능 | 관리자 | 직원 |
|------|--------|------|
| 영수증 등록 | ✅ | ✅ |
| 영수증 전체 조회 | ✅ | 본인 것만 |
| 영수증 수정·삭제 | ✅ 전체 | ✅ 본인·24시간 이내 |
| 세금계산서 전체 | ✅ | ❌ |
| 도면 생성·편집 | ✅ | ❌ |
| 도면 조회 | ✅ | ✅ |
| 제품 등록·수정·삭제 | ✅ | ❌ |
| 제품 조회·스캔 | ✅ | ✅ |
| 직원 초대·관리 | ✅ | ❌ |
| 플랜 변경·결제 | ✅ | ❌ |

---

### X-3. 인증

#### 로그인
- 이메일 + 비밀번호
- JWT Access Token (유효기간 1시간) + Refresh Token (30일)
- Refresh Token 만료 시 재로그인 필요

#### 비밀번호 재설정
1. 이메일 입력 → 재설정 링크 발송
2. 링크 유효기간: 1시간
3. 새 비밀번호 설정 후 기존 모든 세션 무효화

#### 보안 규칙
- 비밀번호: 최소 8자, 영문+숫자 조합 필수
- 5회 연속 로그인 실패 시 10분 잠금
- HTTPS 필수 (HTTP 접근 시 자동 리다이렉트)

---

### X-4. 대시보드

#### 표시 항목

| 위젯 | 내용 | 플랜 |
|------|------|------|
| 이번 달 세금계산서 | 발행 건수 / 공급가액 합계 | Pro 이상 (Free는 잠금) |
| 이번 달 영수증 | 등록 건수 / 금액 합계 | Free 이상 |
| 최근 도면 | 최근 수정된 도면 3개 썸네일 | Pro 이상 (Free는 잠금) |
| 자주 조회된 제품 | 최근 7일 조회 상위 5개 | Free 이상 |
| 이번 달 사용량 | Free 플랜 영수증 건수 잔여량 표시 | Free만 표시 |

---

### X-5. 플랜 업그레이드

#### 플랜 기능 제한 요약
| 기능 | Free | Pro | Business |
|------|------|-----|----------|
| 영수증 등록 | 월 20건 | 무제한 | 무제한 |
| 영수증 내보내기 | ❌ | ✅ | ✅ |
| 세금계산서 | ❌ | ✅ | ✅ |
| 도면 | ❌ | 5개 | 무제한 |
| 직원 계정 | 2명 | 5명 | 무제한 |
| 우선 지원 | ❌ | ❌ | ✅ |

#### 한도 도달 시 UX
- 기능 사용 시도 시 블로킹 팝업 표시
- 팝업 내 플랜 비교 테이블 + 업그레이드 버튼
- 업그레이드 완료 즉시 해당 기능 활성화 (재로그인 불필요)
