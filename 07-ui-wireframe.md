# 07 — UI 와이어프레임

> **문서 상태**: 초안 (Draft)
> **작성일**: 2026-09-11
> **버전**: v0.1
> **연관 문서**: `02-user-stories.md` · `03-feature-spec.md` · `04-system-architecture.md`(반응형 전략) · `06-api-design.md`

---

## 목차

- [화면 인벤토리](#inventory)
- [역할별 내비게이션 구조](#nav)
- [반응형 기준](#responsive)
- [관리자 화면 — PC](#admin-screens)
- [직원 화면 — 모바일](#staff-screens)
- [고객 화면 — 공유 뷰](#customer-screens)
- [공통 UI 패턴](#patterns)

---

<a id="inventory"></a>

## 화면 인벤토리

| 경로 | 화면명 | 역할 | 기기 | 관련 US | 사용 API |
|------|--------|------|------|---------|----------|
| `/register` | 업체 가입 | 없음 | PC/모바일 | X-01 | `POST /api/auth/register` |
| `/login` | 로그인 | 없음 | PC/모바일 | X-03 | `POST /api/auth/login` |
| `/dashboard` | 대시보드 | 관리자 | PC | X-04 | `GET /api/dashboard` |
| `/receipts` | 영수증 목록 | 관리자·직원 | PC/모바일 | A-02 | `GET /api/receipts` |
| `/receipts/new` | 영수증 등록 | 관리자·직원 | 모바일 우선 | A-01 | `POST /api/receipts`, `/presigned-url` |
| `/receipts/:id` | 영수증 상세·수정 | 관리자·직원 | PC/모바일 | A-03 | `GET·PATCH·DELETE /api/receipts/:id` |
| `/clients` | 거래처 목록 | 관리자 | PC | B-01 | `GET /api/clients` |
| `/clients/new`, `/clients/:id` | 거래처 등록·수정 | 관리자 | PC | B-01 | `POST·PATCH /api/clients` |
| `/tax-invoices` | 세금계산서 목록 | 관리자 | PC | B-03 | `GET /api/tax-invoices` |
| `/tax-invoices/new` | 세금계산서 작성 | 관리자 | PC | B-02 | `POST /api/tax-invoices` |
| `/tax-invoices/:id` | 세금계산서 상세 | 관리자 | PC | B-03, B-04 | `GET /api/tax-invoices/:id`, `/revise`, `/cancel` |
| `/drawings` | 도면 목록 | 관리자 | PC | C-01 | `GET /api/drawings` |
| `/drawings/:id` | 도면 에디터 | 관리자 | PC | C-02, C-03, C-04 | `GET/PATCH /api/drawings/:id`, `/versions`, `/share-links`, `/export` |
| `/products` | 제품 검색 | 관리자·직원 | 모바일 최적화 | D-02 | `GET /api/products` |
| `/products/new`, `/products/:id/edit` | 제품 등록·수정 | 관리자 | PC | D-01, D-04 | `POST·PATCH /api/products` |
| `/products/:id` | 제품 상세 | 관리자·직원 | PC/모바일 | D-02 | `GET /api/products/:id` |
| `/scan` | 바코드·QR 스캔 | 관리자·직원 | 모바일 (카메라) | D-03 | `GET /api/products/variants/scan` |
| `/settings/employees` | 직원 관리·초대 | 관리자 | PC | X-02 | `GET/POST /api/auth/employees`, `/invitations` |
| `/settings/plan` | 플랜·결제 | 관리자 | PC | X-05 | (결제 연동, 09번에서 상세) |
| `/invite/:token` | 초대 수락 | 없음 | PC/모바일 | X-02 | `POST /api/auth/invitations/:token/accept` |
| `/share/drawings/:token` | 도면 공유 뷰 | 없음(고객) | PC/모바일 | C-04 | `GET /api/drawings/shared/:token` |

---

<a id="nav"></a>

## 역할별 내비게이션 구조

### 관리자 (PC, 좌측 사이드바 고정)
```
┌ 로고 ─────────────────────────────┐
│ ▸ 대시보드                        │
│ ▸ 영수증                          │
│ ▸ 세금계산서                      │
│   ▸ 거래처                        │
│ ▸ 도면                            │
│ ▸ 제품 카탈로그                   │
│ ─────────────────────────────     │
│ ▸ 직원 관리                       │
│ ▸ 플랜·결제                       │
└────────────────────────────────────┘
```
Free 플랜에서는 "세금계산서"·"도면" 메뉴에 자물쇠 아이콘이 붙고 클릭 시 업그레이드 팝업(X-5)으로 대체된다.

### 직원 (모바일, 하단 탭 2개 + 카메라 FAB)
```
┌──────────────────────────┐
│                           │
│      (화면 영역)          │
│                           │
│                    ⊕      │ ← 스캔 FAB (/scan)
├──────┬──────┬─────────────┤
│ 영수증 │ 제품검색 │        │
└──────┴──────┴─────────────┘
```
직원은 세금계산서·도면·직원관리 메뉴 자체가 노출되지 않는다(X-2 권한 매트릭스 — 없는 메뉴는 숨김, 403으로 막는 게 아니라 애초에 안 보이게).

### 고객 (도면 공유 뷰)
사이드바·메뉴 없음. 링크 접속 시 도면 이미지 + 기본 정보만 보이는 단독 페이지(레이아웃 chrome 없음).

---

<a id="responsive"></a>

## 반응형 기준

`04-system-architecture.md`의 반응형 전략을 화면별로 매핑한다.

| 브레이크포인트 | 대상 화면 | 비고 |
|----------------|-----------|------|
| PC (1280px+) | 세금계산서, 도면 에디터, 직원 관리 | 이 화면들은 모바일 레이아웃을 별도로 만들지 않고 "PC에서만 접근" 안내로 대체 |
| 태블릿 (768px+) | 대시보드, 영수증 목록, 거래처, 제품 등록 | 사이드바가 아이콘 전용으로 축소 |
| 모바일 (360px+) | 영수증 등록, 제품 검색·스캔, 도면 공유 뷰 | 하단 탭 내비게이션으로 전환 |

---

<a id="admin-screens"></a>

## 관리자 화면 — PC

### 대시보드 (`/dashboard`)
```
┌ 사이드바 │ 대시보드                                    [김대표님 ▾] ┐
├──────────┼──────────────────────────────────────────────────────┤
│          │ ┌ 이번 달 세금계산서 ┐ ┌ 이번 달 영수증 ┐ ┌ 사용량(Free)┐│
│          │ │ 12건 · 2,450만원  │ │ 38건 · 312만원 │ │ 18/20건    ││
│          │ └────────────────────┘ └────────────────┘ └────────────┘│
│          │ ┌ 최근 도면 ─────────────────┐ ┌ 자주 조회된 제품 ─────┐│
│          │ │ OO식당 배치도 · 09-11      │ │ 업소용 스텐 냄비 L 26회 ││
│          │ │ △△카페 배치도 · 09-08      │ │ 후라이팬 28cm      19회 ││
│          │ └─────────────────────────────┘ └────────────────────────┘│
└──────────┴──────────────────────────────────────────────────────┘
```
Free 플랜이면 "세금계산서" 위젯이 흐림 처리 + "Pro로 업그레이드" 오버레이(X-4)로 대체.

### 영수증 목록 (`/receipts`)
```
┌ 필터: 기간[      ~      ] 거래처[      ▾] 항목[전체 ▾]  [내보내기] [+ 등록]┐
├────────────────────────────────────────────────────────────────────────┤
│ 날짜        거래처        금액       항목    이미지  등록자             │
│ 09-10      한성식자재    85,000원   재료비   📎     김직원              │
│ 09-09      —            120,000원  운반비   —      박직원              │
│ ...                                                                     │
├────────────────────────────────────────────────────────────────────────┤
│ 합계: 3,120,000원                                    [◂ 1 2 3 ▸]       │
└────────────────────────────────────────────────────────────────────────┘
```

### 세금계산서 작성 (`/tax-invoices/new`)
```
┌ 공급자(자동)          │ 공급받는자 [거래처 검색 ▾]                      ┐
│ 삼진주방설비 · 123-45-67890                                            │
├──────────────────────────────────────────────────────────────────────┤
│ 품목명            규격      수량   단가        공급가액    세액         │
│ 업소용 냉장고 900L  2도어    1     1,800,000   1,800,000   180,000  [-]│
│ 설치비             —        1       150,000     150,000    15,000  [-]│
│                                                          [+ 품목 추가] │
├──────────────────────────────────────────────────────────────────────┤
│ 합계 2,145,000원 (공급가액 1,950,000 + 세액 195,000)                  │
│                                       [임시저장]  [작성완료]           │
└──────────────────────────────────────────────────────────────────────┘
```
품목 17번째 추가 시도 시 인라인 안내로 차단(B-2).

### 도면 에디터 (`/drawings/:id`)
```
┌ ← 도면 목록   OO식당 배치도            [v4 · 방금 자동저장됨] [저장] [공유] [PDF]┐
├──────────┬─────────────────────────────────────────────┬───────────────┤
│ 기본 심볼 │                                             │ 선택한 오브젝트 │
│ 가스레인지│              (Fabric.js 캔버스)             │ 냉장고 #1      │
│ 싱크대    │                                             │ 라벨 [      ]  │
│ 냉장고    │                                             │ 회전 [  0°  ]  │
│ 작업대    │                                             │ 잠금 ☐         │
│ ──────── │                                             │ [삭제]         │
│ 제품 검색 │                                             │                │
│ [      ] │                                             │                │
└──────────┴─────────────────────────────────────────────┴───────────────┘
```
좌측 사이드바는 `03-feature-spec.md` C-2 심볼 라이브러리(기본 제공) + 제품 카탈로그 검색(아이콘 클릭 배치, `productId` 연결) 두 섹션으로 나뉜다. 우측 패널은 선택된 오브젝트가 있을 때만 나타난다.

---

<a id="staff-screens"></a>

## 직원 화면 — 모바일

### 제품 검색 (`/products`) — "모바일 최적화 기준" 화면
```
┌───────────────────────┐
│ ← 제품 카탈로그        │
├───────────────────────┤
│ [ 대분류 ▾ ] [사이즈▾] │
├───────────────────────┤
│ ┌────┐ 업소용 스텐 냄비│
│ │사진│ L · 42,000원    │
│ └────┘                │
│ ┌────┐ 후라이팬 28cm   │
│ │사진│ 39,000원        │
│ └────┘                │
├───────────────────────┤
│ [🔍 제품명·재질 검색  ]│ ← 하단 고정
├──────┬──────┬──────────┤
│영수증 │제품검색│    ⊕   │
└──────┴──────┴──────────┘
```
- 검색창은 화면 하단에 고정(엄지 조작 범위, D-2 "하단 고정 검색창, 카드형 목록").
- 카드 목록은 1열, 썸네일 좌측 고정 크기로 스크롤 시 흔들리지 않게 한다.
- 검색 결과 없음 시: 카드 목록 자리에 "유사 카테고리 제품 3개" 대체 표시(D-2).

### 스캔 결과 팝업 (`/scan`)
```
┌───────────────────────┐
│      (카메라 미리보기)  │
│                       │
└───────────────────────┘
   ↓ 인식 성공
┌───────────────────────┐
│ ✕                     │
│ ┌────┐ 업소용 스텐 냄비│
│ │사진│ L · 42,000원    │
│ └────┘ 스테인리스      │
│ 대량 조리용             │
│ ⚠ 직화 전용, 인덕션 불가│
└───────────────────────┘
```
매칭 실패 시 팝업 문구가 "등록되지 않은 제품입니다"로 바뀌고, 관리자 계정에만 "제품 등록" 버튼이 추가로 보인다(D-3).

### 영수증 등록 (`/receipts/new`)
```
┌───────────────────────┐
│ ← 영수증 등록          │
├───────────────────────┤
│ [ 📷 사진 촬영 ]        │
│ [ 🖼 갤러리에서 선택 ]  │
├───────────────────────┤
│ 날짜   [2026-09-11]    │
│ 금액   [        ]원    │
│ 거래처 [        ]      │
│ 항목   [재료비 ▾]      │
│ 메모   [        ]      │
├───────────────────────┤
│      [ 저장 ]          │
└───────────────────────┘
```

---

<a id="customer-screens"></a>

## 고객 화면 — 공유 뷰 (`/share/drawings/:token`)

```
┌─────────────────────────────────────────┐
│  OO식당 주방 배치도                       │
│  고객명: OO식당   작성일: 2026-09-11      │
├─────────────────────────────────────────┤
│                                           │
│           (도면 이미지, 읽기 전용)        │
│                                           │
├─────────────────────────────────────────┤
│              [ PDF로 저장 ]              │
└─────────────────────────────────────────┘
```
로그인 버튼, 메뉴, 편집 도구 일체 없음. 링크 만료/비활성화 시 이 레이아웃 대신 안내 문구 1줄짜리 페이지로 대체(`06-api-design.md`의 `SHARE_LINK_EXPIRED`/`SHARE_LINK_REVOKED`).

---

<a id="patterns"></a>

## 공통 UI 패턴

| 패턴 | 트리거 | 동작 |
|------|--------|------|
| 플랜 한도 팝업 | Free/Pro 한도 초과 기능 시도 (`PLAN_LIMIT_EXCEEDED`) | 모달로 현재 사용량 + 플랜 비교표 + 업그레이드 버튼 표시, 기능 진행은 차단 (X-5) |
| 삭제 확인 다이얼로그 | 영수증·제품·거래처 등 삭제 버튼 클릭 | "취소/삭제" 2버튼 다이얼로그, 삭제는 위험 색상으로 구분 |
| 빈 상태(Empty state) | 목록 필터 결과 0건 | "해당 조건의 OO가 없습니다" + 필터 초기화 버튼 |
| 낙관적 자동저장 표시 | 도면 에디터 자동저장 | 상단에 "방금 자동저장됨" 텍스트만 표시, 별도 팝업 없음(작업 방해 금지) |
| 소프트 삭제 안내 | 삭제된 리소스 재접근(스캔 등) | "단종/삭제된 항목입니다" 안내로 대체, 404 대신 명확한 사유 표기 |

---

## 다음 단계

1. `08-deployment.md` — 배포 전략·인프라·CI/CD
2. `09-security.md` — 인증·인가·암호화·접근 제어 상세
